/* import React from 'react';
import Wrapper from 'components/Product/ProductWrapper';

const Product = (product) => {
  const { media, assets, price, name, inventory, id } = product.product;
  return <Wrapper />;
};

export default Product;

 */
